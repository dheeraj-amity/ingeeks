# Copy to .env.local and fill values

# Vercel Blob (optional for contact messages cloud persistence)
# Create token: vercel blob tokens create ingeeks-blob --read-write
# Add to Vercel project environment variables.
BLOB_READ_WRITE_TOKEN=
CONTACT_BLOB_ACCESS=public # or private
